# OS / editor noise
Thumbs.db
Desktop.ini
.DS_Store
*.tmp
*.bak
~$*

# Copilot Studio local sync state (contains tenant IDs, endpoints, secrets) — never commit
**/.mcs/

# Secrets / local environment values
*.secret
*.secrets
.env
.env.*
appsettings.local.json

# Packaged agent artifacts
*.zip